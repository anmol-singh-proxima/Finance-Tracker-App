/**
 * Chart series colors — the JS-side counterpart of the CSS design tokens
 * (TR-UX-01); Recharts needs literal values, so the data-viz palette is
 * defined once here rather than scattered through chart components.
 */

export const CHART_PRIMARY = '#2563eb';

/** Categorical palette; series cycle through it. */
export const CHART_SERIES = [
  '#2563eb',
  '#16a34a',
  '#f59e0b',
  '#dc2626',
  '#7c3aed',
  '#0891b2',
  '#db2777',
];
